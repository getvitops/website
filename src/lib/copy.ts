/**
 * Shared copy fragments that appear on many pages.
 *
 * Centralised so a wording change is one edit, not a dozen — and so the funding
 * nudge stays identical everywhere it appears, which is the point of it.
 */

/**
 * The line beside every CTA. Deliberately soft and non-technical: naming DMAP
 * and TDP up front assumes the reader knows what they are and quietly excludes
 * anyone outside Ontario. The link carries the detail.
 */
export const FUNDING_NOTE =
  'Ask us if <a class="link" href="/funding">funding</a> might be available.';

/**
 * The slim callout band above the fold. Jurisdiction-neutral: federal programs
 * apply anywhere in Canada, and the provincial picture depends on where the
 * client operates — not on where we are.
 */
export const FUNDING_CALLOUT =
  "Government programs may cover part of this work — federal, and provincial depending on where you operate.";
